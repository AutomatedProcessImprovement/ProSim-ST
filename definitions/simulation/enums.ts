export enum ElementTypes {
    FLOW = 'bpmn:SequenceFlow',
    TASK = 'bpmn:Task',
}

export enum LifecycleTypes {
    START = 'START',
    ENABLED = 'ENABLED',
    COMPLETE = 'COMPLETE',
    END = 'END',
}
